# StackHost configuration for manhwa-bot
name: manhwa-bot
type: worker                     # background worker, not a web service
runtime:
  engine: docker                 # Docker‑based deployment
  dockerfile: Dockerfile         # path to your Dockerfile (default is fine)

# Environment variables – leave values empty, fill them in the StackHost dashboard
env:
  BOT_TOKEN: ""
  CHAT_ID: ""
